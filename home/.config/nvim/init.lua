require('vim_config') -- core options: leader key, indentation, line numbers, clipboard, etc.
require('plugin')     -- bootstraps lazy.nvim and loads every plugin spec in lua/plugins/
require('keys')       -- custom keymaps (save on Esc, select-all, paste without clobbering clipboard)
